<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Register</title>

    <style>
        /* Styles for the body element */
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            font-family:'poppins', sans-serif;
        }

        /* Styles for the form container */
        .form-container {
          width: 400px;
          margin: auto;
          background-color: #d3cfcf;
          padding: 20px;
          border-radius: 10px;
          box-shadow: 0px 0px 10px #888888;
        }
        
        /* Styles for the form inputs */
        input[type=text], input[type=password], input[type=number] {
          width: 100%;
          padding: 12px 20px;
          margin: 8px 0;
          display: inline-block;
          border: 1px solid rgb(190, 190, 190);
          border-radius: 4px;
          box-sizing: border-box;
        }
        
        /* Styles for the submit buttons */
        input[type=submit] {
          background-color:rgba(0, 60, 255, 0.788) ;
          color: white;
          padding: 12px 20px;
          border: none;
          border-radius: 4px;
          cursor: pointer;
          width: 100%;
        }
        
        /* Styles for the link to switch between the registration and login forms */
        .form-container a {
          color: #2196F3;
        }
        
        /* Styles for the form header */
        .form-header {
          text-align: center;
          margin-bottom: 20px;
        }
        
        /* Styles to hide the login form by default */
        .login-form {
          display: none;
        }

        #user_error,
        #pass_error {
          margin-top: 5px;
          width: 345px;
          font-size: 18px;
          color: #c62828;
          background: rgba(255,0,0,0.1);
          text-align: center;
          padding: 5px 8px;
          border-radius: 3px;
          border: 1px solid #EF9A9A;
          display:none;
        }

      </style>
    </head>
    <body>
      <div class="form-container">
        <h2 class="form-header">Registration Form</h2>

        <ul>
          <% if(typeof errors !='undefined') { %>
            <% errors.forEach(error =>{ %>
              <li><%= errors.message %></li>
           <% }) %>
          <%} %>
        </ul>

        <form name="form" action="/register" method="POST">
          <label for="full name"><b>Full name</b></label>
          <input type="text" placeholder="Enter  Full name" name="name" id="fullName" required>
          <label for="username"><b>Username</b></label>
          <input type="text" placeholder="Enter Username" name="username" id="newUser" required>
          <label for="email"><b>Email</b></label>
          <input type="text" placeholder="Enter Email" name="email" id="newEmail" required>
          <label for="address"><b>Address</b></label>
          <input type="text" placeholder="Enter Address" name="address" id="address" required>
          <label for="number"><b>Phone Number</b></label>
          <input type="tel" placeholder="Enter Number" name="phone" id="phoneNumber" required>
          <label for="password"><b>Password</b></label>
          <input type="password" placeholder="Enter Password" name="password" id="newPassword" required>
          <label for="password"><b>Confirm Password</b></label>
          <input type="password" placeholder="Retype Password" name="password2" required>
          <input type="submit" value="Register" id="regbutton" onclick="registerUser()">
        </form>
        <p>Already have an account? <a href="/login" onclick="toggleForms()">Login here</a>.</p>
      </div>

      <script src="login.js"></script>
</body>
</html>